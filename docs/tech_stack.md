# Tech Stack — KITSU

## 📌 Назначение документа

Этот файл фиксирует **технологический стек проекта KITSU** и является **обязательным для соблюдения**.

Цель:
- исключить произвольную замену технологий
- предотвратить архитектурные ошибки
- дать AI-агенту однозначные инструкции

Любые изменения стека допускаются **только через обновление этого документа**.

---

## 🧩 Общая архитектура

Проект состоит из двух независимых приложений:

- **Frontend** — клиентское web-приложение
- **Backend** — API и бизнес-логика

Коммуникация осуществляется **исключительно через HTTP API**.

---

## 🎨 Frontend Stack

### Основной фреймворк
- **Next.js** (App Router)
  - используется как SSR + SPA
  - поддержка PWA
  - server actions **не используются** для бизнес-логики

Причина выбора:
- SEO
- высокая производительность
- зрелая экосистема

---

### Язык
- **JavaScript (ES2022+)**

TypeScript **не используется** в текущей версии проекта.

---

### UI и стили
- **TailwindCSS** — utility-first стилизация
- **shadcn/ui** — базовые UI-компоненты

Правила:
- запрещено использовать CSS-фреймворки помимо Tailwind
- запрещено inline-стилирование

---

### Видео и плеер
- **HLS.js** — воспроизведение HLS-потоков

Используется для:
- адаптивного битрейта
- fallback для Safari
- контроля ошибок воспроизведения

---

### PWA
- Web App Manifest
- Service Worker

Используется для:
- offline-кеша UI
- быстрого старта приложения

Видео-контент **не кешируется**.

---

## ⚙️ Backend Stack

### Язык и фреймворк
- **Python 3.11+**
- **FastAPI**

Причина выбора:
- асинхронность
- строгая типизация данных
- OpenAPI-совместимость

---

### Асинхронность
- asyncio

Все I/O операции должны быть **неблокирующими**.

---

### База данных
- **PostgreSQL**

Используется для:
- хранения метаданных
- связей аниме / эпизодов
- пользовательских данных

---

### ORM
- **Async ORM** (SQLAlchemy async)

Правила:
- синхронный ORM запрещён
- raw SQL допустим только в критичных местах

---

### Кеширование
- **Redis**

Используется для:
- кеша API-ответов
- rate-limiting
- session storage (опционально)

---

## 🔐 Безопасность и инфраструктура

### HTTP
- HTTPS обязателен
- CORS строго ограничен

### Secrets
- все ключи хранятся в ENV
- запрещено хранение секретов в коде

---

## 🧪 Тестирование

- unit tests
- integration tests

Инструменты:
- pytest
- httpx (async)

---

## 🚫 Запрещено

- менять стек без обновления документации
- добавлять новые технологии «для удобства»
- использовать сторонние видео-плееры
- хранить видео или проксировать потоки

---

## 🧠 Правила для AI-агента

AI-агент обязан:
- использовать только указанный стек
- не предлагать альтернативы
- не менять версии и подходы

При необходимости изменений — **запрашивать обновление tech-stack.md**.

---

## 📌 Статус документа

- обязательный
- канонический
- source of truth

