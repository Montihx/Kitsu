Development Guide — KITSU

📌 Назначение документа

Этот документ описывает строгий процесс разработки KITSU.
Он обязателен для всех участников и AI-агентов (Copilot / Agent mode).

Цели:
	•	исключить хаотичную разработку
	•	предотвратить баги и уязвимости
	•	обеспечить production-grade качество
	•	сделать поведение AI детерминированным

README.md и docs/ являются source of truth.

⸻

1️⃣ Общие принципы разработки

1.1 Бизнес-уровень качества
	•	код пишется как для production с первого коммита
	•	отсутствие TODO, FIXME, временных решений
	•	никакой логики “потом поправим”

⸻

1.2 Запреты

❌ менять архитектуру без обновления docs
❌ добавлять эндпоинты без api-reference.md
❌ обращаться к Kodik / Shikimori напрямую из frontend
❌ хранить секреты в коде
❌ добавлять новые технологии без tech-stack.md

⸻

2️⃣ Правила для AI-агента

AI-агент обязан:
	•	реализовывать код строго по md-документам
	•	не додумывать поведение
	•	не упрощать функционал
	•	при неоднозначности останавливаться и запрашивать документ

Любое отклонение считается критической ошибкой.

⸻

3️⃣ Backend Development

3.1 Структура

Backend обязан следовать структуре из repo-structure.md.

Ключевые правила:
	•	api/ — только HTTP-слой
	•	services/ — бизнес-логика
	•	repositories/ — работа с БД
	•	parsers/ — только интеграции с провайдерами

Зависимости строго однонаправленные.

⸻

3.2 Добавление нового API-эндпоинта

Порядок действий:
	1.	Обновить docs/api-reference.md
	2.	Зафиксировать request / response
	3.	Реализовать endpoint в api/
	4.	Реализовать бизнес-логику в services/
	5.	Добавить тесты

Без шага 1 реализация запрещена.

⸻

3.3 Добавление нового провайдера
	1.	Создать папку в parsers/
	2.	Реализовать client + schemas + mapper
	3.	Обновить документацию провайдера
	4.	Подключить через backend services

Frontend не знает о провайдерах.

⸻

4️⃣ Frontend Development

4.1 Общие правила
	•	frontend — UI и UX, не бизнес-логика
	•	данные приходят только из backend API
	•	никакой прямой работы с HLS-провайдерами

⸻

4.2 Работа с API
	•	использовать единый API-клиент
	•	строго следовать api-reference.md
	•	не обрабатывать “лишние” поля

⸻

5️⃣ Код-стайл

Backend (Python)
	•	Python 3.11+
	•	async / await везде
	•	типизация обязательна
	•	функции ≤ 50 строк

Frontend (JS)
	•	ES2022+
	•	функциональные компоненты
	•	hooks вместо классов

⸻

6️⃣ Тестирование

Обязательные типы тестов
	•	unit tests
	•	integration tests

Инструменты:
	•	pytest
	•	httpx (async)

❌ отсутствие тестов — блокер

⸻

7️⃣ CI / CD (базовые требования)

Pipeline обязан:
	•	запускать тесты
	•	проверять линтер
	•	блокировать merge при ошибках

⸻

8️⃣ Безопасность
	•	валидация всех входных данных
	•	защита от XSS / CSRF / brute-force
	•	rate-limiting включён всегда

⸻

9️⃣ Работа с документацией
	•	docs обновляются вместе с кодом
	•	устаревшая документация запрещена
	•	при расхождении docs > code

⸻

📌 Статус документа
	•	канонический
	•	обязательный
	•	production-grade
