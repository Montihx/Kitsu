# AI Guidelines — KITSU

## 📌 Назначение документа

Этот документ фиксирует **правила работы AI-агентов (Copilot / Agent)** с репозиторием KITSU.
Документ является **обязательным для исполнения**.

Все действия AI должны строго соответствовать этим правилам.

---

## 1️⃣ Основные принципы

1. **Не додумывать требования** — AI использует только информацию из md-файлов.
2. **Не упрощать функционал** — минимальный и максимальный функционал должен быть реализован.
3. **Не нарушать архитектуру** — структура frontend / backend / docs обязательна.
4. **Безопасность превыше всего** — ключи, CORS, rate-limit, XSS, CSRF, HLS поток.
5. **Источники данных** — только Shikimori (метаданные) и Kodik (видео).
6. **При отсутствии инструкции** — AI обязан запросить соответствующий md-файл, а не импровизировать.

---

## 2️⃣ Работа с репозиторием

- Папки `333/` и `idea/` **не используются** и полностью удалены.
- Любые новые папки или файлы добавляются только после согласования и создания соответствующего md.
- README.md и docs/ являются source of truth.

---

## 3️⃣ Генерация кода

- Каждая задача — один шаг.
- Каждая функция или endpoint должна строго соответствовать API-contract в `api-reference.md`.
- Названия классов, методов и переменных должны соответствовать style-guide из `development.md`.
- Никаких TODO или заглушек в production-grade коде.

---

## 4️⃣ Безопасность

- **Ни один ключ или секрет не должен попасть в frontend.**
- Валидация всех входных данных.
- Защита от XSS, CSRF, rate-limit, brute-force.
- HLS-потоки должны обрабатываться через backend.

---

## 5️⃣ Тестирование

- AI обязан генерировать unit и integration tests для каждого модуля.
- Все тесты должны быть совместимы с pytest и httpx async.

---

## 6️⃣ Документация

- Все изменения кода должны сопровождаться обновлением соответствующего md-файла.
- Если файл отсутствует — запросить его создание.
- Ни один AI-агент не имеет права импровизировать или изменять функционал без документации.

---

## 7️⃣ Локализация и данные

- Все тексты и названия берутся из Shikimori.
- Не создавать альтернативные источники данных без md.

---

## 8️⃣ Ответственность AI

- Нарушение этих правил считается критической ошибкой.
- Любые отклонения от инструкций запрещены.
- Вся генерация кода должна быть deterministic и predictable.

---

## 📌 Заключение

`ai-guidelines.md` — **канонический документ**.  
Он определяет поведение AI-агента и обеспечивает **безопасность, предсказуемость и production-grade качество проекта KITSU**.

